#!/usr/bin/with-contenv bashio
# ==============================================================================
# PostgreSQL crashed — halt the container so HA Supervisor can restart
# ==============================================================================
bashio::log.error "PostgreSQL exited unexpectedly (exit code: ${1}), halting container..."

exec /run/s6/basedir/bin/halt
